VITE_API_URL=http://localhost:8000/api
# Chiave pubblica VAPID generata lato backend con `php artisan webpush:vapid`
# (vedi VAPID_PUBLIC_KEY nell'.env del backend) — è pubblica, non un segreto.
VITE_VAPID_PUBLIC_KEY=
